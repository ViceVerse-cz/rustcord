# Synthetic native evidence

Baseline: rebuilt Windows text package from 74c0d79, launched explicitly with --demo. before.png shows the existing conversation and reactions before the Pins control existed. A baseline reaction toggle changed 3 to 4 without hiding messages; this synthetic path bypasses HTTP and cannot reproduce the owner's service failure.

The owner stopped Computer Use with the physical Escape key before after-change capture. No further desktop automation was performed. The after image and native Pins interaction remain pending; headless light/dark narrow-width keyboard tests are separate evidence. No live account or private content appears in this image.
